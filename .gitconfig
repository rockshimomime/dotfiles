[user]
    name = rockshimomime
    email = sh.alencar@rocketmail.com
[core]
    editor = code --wait
[init]
    defaultBranch = main
[alias]
    s = !git status -s
    l = !git log --pretty=format:'%C(blue)%h%C(red)%d %C(white)%s - %C(cyan)%cn, %C(green)%cr'

