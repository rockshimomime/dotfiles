- hosts: localhost
  become: yes
  tasks:
    - name: Update pacman cache 
      pacman:
        update_cache: yes

    - name: Install desired packages
      pacman:
        name:
          - git
          - neovim
          - tmux
          - alacritty
          - fish
          - docker
          - kubectl
          - azure-cli
          - aws-cli
          - exa
          - curl
          - dog
          - bat
          - jq
          - yq
          - gping
          - vlc
        state: present
